apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: network-policy-for-namespace-c
  namespace: namespace-c
spec:
  # TODO:
  # Define ingress
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress # prohibit outbound traffic (Principle of Least Privilege)
  ingress:
    - from:
      - namespaceSelector:
        matchLabels:
          name: namespace-a