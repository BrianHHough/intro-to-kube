apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  # TODO: 
  # - Add your nginx-pod spec with label `just-a-pod` and containerPort 80
  labels:
    just-a-pod: "true"
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
      - containerPort: 80